
```
#!/bin/sh
# /etc/init.d/myapp_daemon
# 【定制版】APP后台运行 + 脚本监控崩溃 + 自动重启

# ==================== 配置区 (只改这里) ====================
APP_NAME="myapp"
APP_PATH="/usr/bin/myapp"
APP_ARGS=""          # APP参数
PID_FILE="/tmp/${APP_NAME}.pid"
DAEMON_PID_FILE="/tmp/${APP_NAME}_watch.pid"
LOG_FILE="/tmp/${APP_NAME}_run.log"
# 日志大小限制
MAX_LOG_SIZE=$((1024 * 1024))
# ==========================================================

# 日志清理
clean_log() {
    if [ -f "$LOG_FILE" ] && [ $(stat -c "%s" "$LOG_FILE") -gt $MAX_LOG_SIZE ]; then
        tail -n 100 "$LOG_FILE" > "${LOG_FILE}.tmp" && mv "${LOG_FILE}.tmp" "$LOG_FILE"
    fi
}

# 检查APP是否正在运行
is_app_running() {
    pidof -x "$(basename $APP_PATH)" >/dev/null 2>&1
}

# 启动APP（后台运行！）
start_app() {
    clean_log
    echo "[$(date)] 后台启动 APP" >> "$LOG_FILE"
    # 👇 关键：APP 后台运行 & ，完全脱离终端
    $APP_PATH $APP_ARGS &
}

# 监控循环（后台运行，不占串口）
start_watch() {
    if [ -f "$DAEMON_PID_FILE" ]; then
        echo "监控进程已运行"
        return 0
    fi

    echo "启动 APP 后台监控..."
    # 后台循环监控，绝对不占用串口
    (
        while true
        do
            # 检查APP是否存活
            if ! is_app_running; then
                echo "[$(date)] APP 崩溃，自动重启" >> "$LOG_FILE"
                start_app
            fi
            # 每隔1秒检查一次
            sleep 1
        done
    ) &
    echo $! > "$DAEMON_PID_FILE"
}

# 主启动
start() {
    # 先启动APP
    start_app
    # 再启动监控
    start_watch
    echo "APP 后台运行 + 监控启动完成"
}

# 停止：关闭监控+关闭APP
stop() {
    echo "停止监控和 APP..."
    # 杀监控
    if [ -f "$DAEMON_PID_FILE" ]; then
        kill $(cat "$DAEMON_PID_FILE") 2>/dev/null
        rm -f "$DAEMON_PID_FILE"
    fi
    # 杀APP
    pkill -f "$APP_PATH" 2>/dev/null
    rm -f "$PID_FILE"
    echo "已完全停止"
}

restart() { stop; start; }
status() { is_app_running && echo "APP 运行中" || echo "APP 已停止"; }

case "$1" in
    start|stop|restart|status) $1 ;;
    *) echo "用法: $0 {start|stop|restart|status}" ;;
esac
exit 0
```

# 完整版脚本 + 逐行中文讲解

bash

运行

```
#!/bin/sh
# /etc/init.d/myapp_daemon
# 【定制版】APP后台运行 + 脚本监控崩溃 + 自动重启
```

- `#!/bin/sh`：**脚本声明**，告诉系统用 `/bin/sh` 解析这个脚本（Linux 标准 shell）
- 下面两行是注释，写脚本用途、存放路径，不执行

---

bash

运行

```
# ==================== 配置区 (只改这里) ====================
APP_NAME="myapp"
APP_PATH="/usr/bin/myapp"
APP_ARGS=""          # APP参数
PID_FILE="/tmp/${APP_NAME}.pid"
DAEMON_PID_FILE="/tmp/${APP_NAME}_watch.pid"
LOG_FILE="/tmp/${APP_NAME}_run.log"
# 日志大小限制
MAX_LOG_SIZE=$((1024 * 1024))
# ==========================================================
```

这是**唯一需要你修改的区域**，全是自定义变量：

1. `APP_NAME="myapp"`：你的应用名字
2. `APP_PATH="/usr/bin/myapp"`：APP 的绝对路径
3. `APP_ARGS=""`：APP 启动参数（没有就空着）
4. `PID_FILE="/tmp/${APP_NAME}.pid"`：存 APP 进程 ID 的文件
5. `DAEMON_PID_FILE="/tmp/${APP_NAME}_watch.pid"`：存**监控脚本**进程 ID 的文件
6. `LOG_FILE="/tmp/${APP_NAME}_run.log"`：日志文件路径
7. `MAX_LOG_SIZE=$((1024 * 1024))`：日志最大 1MB，防止写满内存

---

bash

运行

```
# 日志清理
clean_log() {
    if [ -f "$LOG_FILE" ] && [ $(stat -c "%s" "$LOG_FILE") -gt $MAX_LOG_SIZE ]; then
        tail -n 100 "$LOG_FILE" > "${LOG_FILE}.tmp" && mv "${LOG_FILE}.tmp" "$LOG_FILE"
    fi
}
```

**日志清理函数**，防止日志无限变大：

1. 判断日志文件存在 + 大小超过 1MB
2. 只保留最后 100 行日志，覆盖原文件
3. 嵌入式必备，保护内存 / Flash

---

bash

运行

```
# 检查APP是否正在运行
is_app_running() {
    pidof -x "$(basename $APP_PATH)" >/dev/null 2>&1
}
```

**核心功能：判断 APP 是否存活**

1. `pidof`：Linux 命令，查询指定程序的进程 ID
2. `basename $APP_PATH`：提取 APP 名字（比如 myapp）
3. `>/dev/null 2>&1`：不输出任何信息，只返回结果
4. 运行中返回`真`，崩溃 / 未启动返回`假`

---

bash

运行

```
# 启动APP（后台运行！）
start_app() {
    clean_log
    echo "[$(date)] 后台启动 APP" >> "$LOG_FILE"
    # 👇 关键：APP 后台运行 & ，完全脱离终端
    $APP_PATH $APP_ARGS &
}
```

**启动 APP 函数（纯后台）**

1. 先清理过大日志
2. 写入启动日志（带时间）
3. `$APP_PATH $APP_ARGS &`：**关键！**
    
    - 运行你的 APP
    - 末尾 `&` = **后台运行**，不占用串口 / 终端
    - 关闭终端，APP 依然运行
    

---

bash

运行

```
# 监控循环（后台运行，不占串口）
start_watch() {
    if [ -f "$DAEMON_PID_FILE" ]; then
        echo "监控进程已运行"
        return 0
    fi

    echo "启动 APP 后台监控..."
    # 后台循环监控，绝对不占用串口
    (
        while true
        do
            # 检查APP是否存活
            if ! is_app_running; then
                echo "[$(date)] APP 崩溃，自动重启" >> "$LOG_FILE"
                start_app
            fi
            # 每隔1秒检查一次
            sleep 1
        done
    ) &
    echo $! > "$DAEMON_PID_FILE"
}
```

**最核心的监控函数**（全程后台，不占串口）：

1. 判断监控是否已运行，防止重复启动
2. `( ) &`：把整个**无限循环**放到系统后台
3. `while true`：死循环，永久监控
4. `if ! is_app_running`：如果 APP 没运行（崩溃了）
5. 写入崩溃日志 → 重启 APP
6. `sleep 1`：每秒检查 1 次
7. `echo $! > $DAEMON_PID_FILE`：把监控进程的 PID 写入文件，方便后续停止

---

bash

运行

```
# 主启动
start() {
    # 先启动APP
    start_app
    # 再启动监控
    start_watch
    echo "APP 后台运行 + 监控启动完成"
}
```

**总启动入口**：

1. 先启动你的 APP（后台）
2. 再启动监控脚本（后台）
3. 打印启动完成

---

bash

运行

```
# 停止：关闭监控+关闭APP
stop() {
    echo "停止监控和 APP..."
    # 杀监控
    if [ -f "$DAEMON_PID_FILE" ]; then
        kill $(cat "$DAEMON_PID_FILE") 2>/dev/null
        rm -f "$DAEMON_PID_FILE"
    fi
    # 杀APP
    pkill -f "$APP_PATH" 2>/dev/null
    rm -f "$PID_FILE"
    echo "已完全停止"
}
```

**停止函数**（彻底关闭，不残留进程）：

1. 读取监控 PID → 关闭监控循环
2. 删除监控 PID 文件
3. `pkill` 关闭你的 APP
4. 删除 APP PID 文件
5. 彻底停止，不会再自动重启

---

bash

运行

```
restart() { stop; start; }
status() { is_app_running && echo "APP 运行中" || echo "APP 已停止"; }
```

**快捷命令**：

1. `restart`：先停止，再启动
2. `status`：检查 APP 运行状态

---

bash

运行

```
case "$1" in
    start|stop|restart|status) $1 ;;
    *) echo "用法: $0 {start|stop|restart|status}" ;;
esac
exit 0
```

**命令解析器**：

1. `$1`：你输入的参数（start/stop 等）
2. 输入`start` → 执行`start`函数
3. 输入`stop` → 执行`stop`函数
4. 输入其他内容 → 提示用法
5. `exit 0`：脚本正常退出

---

# 脚本整体运行逻辑（极简总结）

1. 执行 `start`
2. **后台启动 APP**
3. **后台启动无限循环监控**
4. 监控每秒检查一次 APP
5. APP 崩溃 → 自动重启
6. 执行 `stop` → 关闭监控 + 关闭 APP
7. 全程**不占用串口**，所有日志写文件

---

# 你最关心的 3 个问题，脚本里都实现了

1. ✅ **APP 纯后台运行**（靠 `&`）
2. ✅ **监控崩溃 + 自动重启**（靠 `while` 循环 + `pidof`）
3. ✅ **不占用串口 / 终端**（监控和 APP 全在后台）


从你提供的运行日志来看，核心问题是 **脚本里用了 `stat` 命令但系统找不到，且 `stat` 命令的语法 / 参数也有错误**，导致脚本执行报错（`stat: command not found` + `[: -gt: unary operator expected`），不过你的 APP 本身也有 `gpio init failed` 等业务报错，我先帮你一步步定位和解决！

### 第一步：先分析报错原因（按优先级排序）

#### 1. 最紧急：嵌入式系统缺少 `stat` 命令（核心脚本报错）

- 报错信息：`./S99myapp.sh: line 18: stat: command not found`
- 原因：你的嵌入式 Linux 系统做了极致精简（比如 busybox 没编译 `stat` 命令），脚本里用了 `stat` 来获取日志文件大小，但系统里根本没有这个命令，导致脚本执行到这一行直接报错。
- 连带报错：`[: -gt: unary operator expected` → 因为 `stat` 执行失败，没拿到日志文件大小，脚本里的 `if [ $(stat -c "%s" "$LOG_FILE") -gt $MAX_LOG_SIZE ];` 变成了空值对比，shell 语法报错。

#### 2. 次要：APP 业务报错（`gpio init failed` 等）

- 报错信息：`Request input failed`、`gpio init failed`
- 原因：这是你 C 程序里的业务逻辑报错（GPIO 初始化失败、输入请求失败），和脚本无关，先解决脚本问题后再排查 APP 本身的硬件 / 配置问题。

### 第二步：解决脚本报错（核心！先让脚本能正常运行）

因为嵌入式系统没有 `stat` 命令，我们**删掉脚本里依赖 `stat` 的日志大小检查逻辑**（换成更简单的方式，或直接注释，嵌入式精简系统优先保证脚本能跑）。

#### 修改后的完整脚本（适配无 `stat` 命令的嵌入式系统）

bash

运行

```
#!/bin/sh
# /etc/init.d/S99myapp.sh
# 适配无stat命令的嵌入式系统：删除stat依赖，保留核心功能

# ==================== 配置区 (只改这里) ====================
APP_NAME="myapp"
APP_PATH="/usr/bin/myapp"  # 改成你实际的APP路径
APP_ARGS=""
PID_FILE="/tmp/${APP_NAME}.pid"
DAEMON_PID_FILE="/tmp/${APP_NAME}_watch.pid"
LOG_FILE="/tmp/${APP_NAME}_run.log"
# 注释日志大小限制（因为没有stat命令）
# MAX_LOG_SIZE=$((1024 * 1024))
# ==========================================================

# 注释日志清理函数（依赖stat，删掉）
# clean_log() {
#     if [ -f "$LOG_FILE" ] && [ $(stat -c "%s" "$LOG_FILE") -gt $MAX_LOG_SIZE ]; then
#         tail -n 100 "$LOG_FILE" > "${LOG_FILE}.tmp" && mv "${LOG_FILE}.tmp" "$LOG_FILE"
#     fi
# }

# 检查APP是否正在运行
is_app_running() {
    # 替换pidof：如果pidof也没有，用 ps | grep （更通用）
    ps | grep -v grep | grep "$(basename $APP_PATH)" >/dev/null 2>&1
}

# 启动APP（后台运行！）
start_app() {
    # 注释clean_log调用
    # clean_log
    echo "[$(date)] 后台启动 APP" >> "$LOG_FILE"
    $APP_PATH $APP_ARGS &
}

# 监控循环（后台运行，不占串口）
start_watch() {
    if [ -f "$DAEMON_PID_FILE" ]; then
        echo "监控进程已运行"
        return 0
    fi

    echo "启动 APP 后台监控..."
    (
        while true
        do
            # 注释clean_log调用
            # clean_log
            if ! is_app_running; then
                echo "[$(date)] APP 崩溃，自动重启" >> "$LOG_FILE"
                start_app
            fi
            sleep 1
        done
    ) &
    echo $! > "$DAEMON_PID_FILE"
}

# 主启动
start() {
    start_app
    start_watch
    echo "APP 后台运行 + 监控启动完成"
}

# 停止：关闭监控+关闭APP
stop() {
    echo "停止监控和 APP..."
    if [ -f "$DAEMON_PID_FILE" ]; then
        kill $(cat "$DAEMON_PID_FILE") 2>/dev/null
        rm -f "$DAEMON_PID_FILE"
    fi
    # 替换pkill：如果pkill没有，用 killall（更通用）
    killall "$(basename $APP_PATH)" 2>/dev/null
    rm -f "$PID_FILE"
    echo "已完全停止"
}

restart() { stop; start; }
status() { is_app_running && echo "APP 运行中" || echo "APP 已停止"; }

case "$1" in
    start|stop|restart|status) $1 ;;
    *) echo "用法: $0 {start|stop|restart|status}" ;;
esac
exit 0
```

#### 关键修改点（适配嵌入式精简系统）：

1. **删掉所有依赖 `stat` 的代码**：包括 `clean_log` 函数、`MAX_LOG_SIZE` 变量、脚本里的 `clean_log` 调用 —— 彻底解决 `stat: command not found`。
2. **替换 `pidof` 为 `ps | grep`**：很多精简系统也没有 `pidof`，`ps | grep` 是嵌入式通用的进程检查方式。
3. **替换 `pkill` 为 `killall`**：同理，`killall` 在 busybox 里更通用。

### 第三步：重新部署脚本（确保生效）

1. 把修改后的脚本覆盖到 `/etc/init.d/S99myapp.sh`：
    
    bash
    
    运行
    
    ```
    # 上传脚本到板子（比如用scp）
    scp 本地脚本路径 root@板子IP:/etc/init.d/S99myapp.sh
    ```
    
2. 重新加可执行权限：
    
    bash
    
    运行
    
    ```
    chmod +x /etc/init.d/S99myapp.sh
    ```
    
3. 重新启动脚本：
    
    bash
    
    运行
    
    ```
    /etc/init.d/S99myapp.sh stop  # 先停掉旧的
    /etc/init.d/S99myapp.sh start # 启动新的
    ```
    

### 第四步：排查 APP 的业务报错（`gpio init failed`）

脚本问题解决后，再看 APP 的报错：

1. **检查 GPIO 配置文件**：日志里提到 `file path: /etc/rtu_config/config.def.json`，确认这个文件存在、路径正确、配置格式合法（比如 JSON 语法没错）。
2. **检查 GPIO 硬件权限**：嵌入式里 GPIO 操作需要 root 权限，确认你的 APP 是用 root 运行（默认是，除非特意改了用户）。
3. **检查 GPIO 引脚定义**：确认 C 程序里的 GPIO 引脚号和板子实际引脚对应（比如板子的 GPIO5 对应程序里的编号是否正确）。

### 关键验证：脚本是否正常运行

执行以下命令，看是否还有脚本报错：

bash

运行

```
# 查看脚本日志
cat /tmp/myapp_run.log
# 查看进程
ps | grep myapp
```

如果日志里只有 `后台启动 APP`、没有 `stat: command not found`，说明脚本问题解决；APP 的业务报错需要单独排查硬件 / 配置。

### 总结

1. 核心问题：嵌入式系统缺少 `stat` 命令，导致脚本日志检查逻辑报错，连带触发 shell 语法错误。
2. 解决方法：删掉脚本里依赖 `stat` 的代码，替换 `pidof/pkill` 为嵌入式通用命令。
3. 后续步骤：先让脚本正常运行，再排查 APP 的 GPIO 初始化失败问题（配置文件 / 硬件引脚 / 权限）。